import { Fragment } from "react";
import NetflixLogin from "../Netflix/NetflixLoginPage/NetflixLogin";

const Login = () => {
  return (
    <Fragment>
      <NetflixLogin />
    </Fragment>
  );
};

export default Login;
